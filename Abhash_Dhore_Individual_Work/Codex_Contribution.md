# Individual Contribution: Codex

This folder explicitly showcases the individual development work performed by **Codex** on the overall application.

## 1. Database Implementation (`Database/`)
- Setup and designed the SQLite database structure required for the application's backend.
- The `app.db` file within the Database folder contains all the relational schemas and localized records configured for the project.

## 2. Environment Configuration (`Environment_Setup/`)
- Setup the entire Python server environment.
- Configured and generated the `requirements.txt` file which dictates the precise dependency versions (e.g. Flask, SQLAlchemy) for cross-platform compatibility.
- Implemented secure Git practices by standardizing `.gitignore` (ignoring `.venv` and `__pycache__` to adhere to source-control best practices).
